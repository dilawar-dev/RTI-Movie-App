import Fonts from './Fonts';

const Metrics = {
  Radius: {
    Low: Fonts.RF(8),
    Normal: Fonts.RF(15),
    High: Fonts.RF(30),
    Circle: Fonts.RF(100),
  },
  Spacing: {
    Tiny: Fonts.RF(4),
    Small: Fonts.RF(8),
    Medium: Fonts.RF(12),
    Regular: Fonts.RF(16),
    Large: Fonts.RF(18),
    Huge: Fonts.RF(20),
    Mega: Fonts.RF(24),
  },
};

export default Metrics;
